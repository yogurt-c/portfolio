DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# iron-session 쿠키 암호화 키 (32자 이상 무작위). 'make env' 가 자동 생성.
SESSION_PASSWORD=""

# bcrypt 해시. 'make admin-pw' 로 생성. (평문 저장 금지)
ADMIN_PASSWORD_HASH=""

# Vercel Blob 토큰. Vercel 대시보드 → Storage → Blob 에서 발급.
BLOB_READ_WRITE_TOKEN=""
